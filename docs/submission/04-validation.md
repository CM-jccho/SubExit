> 이전 버전 기록입니다. 현재 제출 문안과 상태는 [08-final-submission.md](08-final-submission.md)를 우선 확인하세요.

# 구현 검증 기록

기준 main `8e8ca4b89337c93f3872f5bad3dabb914fc44cb6` (#23). 변경 브랜치명 `codex/hackathon-coach-upgrade`를 사용한다.

| 항목 | 결과 |
|---|---|
| 코어/API 자동 테스트 | 복구 코드 기준 14개 통과, 0개 실패 |
| TypeScript | `npm run typecheck` 통과 |
| 프로덕션 빌드 | Next.js 15.5.25 빌드 성공, 루트 First Load JS 약 115 kB |
| 모의 Gemini | 키가 URL에 포함되지 않음, 동의 누락 시 전송 없음, AI 출처, 429, 잘못된 원문 인용, 음성 전사 반환 확인 |
| 로컬 HTTP smoke | 명시적 localhost 호스트로 production 서버 기동: 홈/demo/live/evidence 4개 URL HTTP 200, 비활성 API 설정 확인 |
| 실제 Gemini 호출 | 미실행: 실제 API 키 없음 |
| 마이크·기기 호환 | 미실행: 실제 기기 테스트 필요 |
| 새 화면 시각 QA | 로컬 주소 접근 ERR_BLOCKED_BY_CLIENT. 미완료 |
| GitHub 저장/PR | 초기 요청 403. 사용자가 SubExit 접근을 추가한 뒤 설치 허용 목록 확인. 최신 업로드 상태는 PR 참고 |
| 운영 배포/대회 제출 | 미실행 |

이전 임시 작업 환경이 사라진 뒤 최신 원본과 남은 작업 기록으로 복구했다. 이전 빌드 결과를 복구본의 결과로 사용하지 않았으며 테스트와 빌드를 다시 실행했다. 모델 응답은 모의 transport이므로 실제 응답 품질의 증거가 아니다.

로컬 서버의 호스트 자동 탐색은 실행 환경의 `uv_interface_addresses` 오류로 실패했다. 호스트를 명시한 `next start --hostname 127.0.0.1`로 실제 HTTP 응답은 확인했다. 이는 브라우저 UI 상호작용 검증을 대신하지 않는다.

## 테스트가 확인하는 것

보상 중복 방지, 수정 선행 조건, 현지 날짜·연속 기록·누적 XP, 잘못된 저장소 데이터, 7개 시나리오 무결성, 면접·발표의 거절 멘트 오적용 방지, 원문 인용 검증, 샘플 표기, API 비활성 오류, 입력 크기, 이전 업로드의 가짜 분석 차단, 무료 API 전송 확인, 제공사 오류 처리.

## 제출 전 남은 검증

1. AI Studio 키로 개인정보 없는 한국어 샘플 20개: 전사 적합성, 목적에 맞는 제안, 원음 종료→화면 표시 시간. 평균뿐 아니라 늦은 응답도 기록.
2. Android Chrome/iOS Safari/Desktop Chrome: 권한 거절, 녹음 조기 중단, 8초 자동 중단, 탭 비활성화, 페이지 이동, 통화 수신 중단.
3. 데스크톱과 모바일 시각 QA: 좁은 화면 줄바꿈, 결과 가독성, 키보드 접근, 화면 낭독. 현재 코드의 반응형 CSS만으로 완료 판정하지 않음.
4. 동시 사용자와 공개 트래픽: 현재 인스턴스별 제한의 한계, 제공사 일별 한도, 지역·연령 조건.
5. 미리보기 배포 확인 후 main 병합/운영 배포 선택. 현재 기존 URL에는 이 변경본이 없다.
